# --- MONITORS --- #
monitor=DP-3,2560x1440@180,0x0,1


# --- KEYBOARD --- #
input {
    kb_layout = us,ru
    kb_options = grp:alt_shift_toggle
    touchpad {
        natural_scroll = no
    }
    sensitivity = 0
}


# --- STARTING PROGRAMS --- #
exec-once = swww-daemon
exec-once = waybar

# --- DECORATIONS --- #
general {
    border_size = 3
    col.active_border = rgb(ffffff)
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}
decoration {
    active_opacity = 0.65	
    rounding = 16    
    inactive_opacity = 0.65
    fullscreen_opacity = 1.0
    blur {
        enabled = true
        size = 10
        passes = 3
        new_optimizations = true
    }
}

# --- ANIMATIONS --- #
animations {
    enabled = yes
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = softSnap, 0.1, 1, 0.1, 1

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, myBezier, slide
}


# --- KEYBINDS --- #
$mainMod = SUPER
bind = $mainMod, Q, exec, kitty
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, thunar
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, rofi -show drun -show-icons
bind = $mainMod, P, pseudo,
bind = $mainMod, J, togglesplit,

# --- SCREENSHOTS --- #
bind = , Print, exec, grim - | wl-copy && wl-paste > ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png
bind = SHIFT, Print, exec, grim -g "$(slurp)" - | wl-copy && wl-paste > ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png
bind = CTRL, Print, exec, grim -g "$(slurp)" - | swappy -f -

#--- MOVE FOCUS --- # 
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# --- WINDOW MOVE --- # 
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

# --- WORKSPACES BINDS --- #
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2

# --- CONFIG RELOAD --- #
bind = $mainMod SHIFT, R, exec, hyprctl reload

